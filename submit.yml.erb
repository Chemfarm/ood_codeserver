---
batch_connect:
  template: "basic"
  conn_params:
    - code_server_version
script:
  native:
   - "-q"
   - "<%= queue %>"
   - "-l"
   - "select=1:ncpus=<%= num_cpus %>:mem=<%= mem %>GB<%= queue == 'gpuq' ? ":ngpus=#{num_gpus}" : "" %>"

